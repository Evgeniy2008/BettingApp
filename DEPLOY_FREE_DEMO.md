# 🆓 Бесплатное развертывание для демо

## ✅ Хорошая новость!

У вас **все уже вместе** в одном проекте:
- ✅ **Telegram бот** (Telegraf)
- ✅ **API для парсинга** (Express сервер)
- ✅ **Парсеры** (Puppeteer + Cheerio)

Все это работает в **одном Node.js приложении** (`bot/src/index.ts`)!

---

## 🎯 Варианты бесплатного хостинга

### Вариант 1: Render.com (РЕКОМЕНДУЕТСЯ) ⭐

**Плюсы:**
- ✅ Полностью бесплатно
- ✅ Автоматический деплой из Git
- ✅ HTTPS из коробки
- ✅ Простая настройка

**Минусы:**
- ⚠️ Сервис "засыпает" после 15 минут (но можно разбудить)

**Инструкция:**

1. **Создайте репозиторий на GitHub:**
   - Загрузите папку `bot/` в репозиторий
   - Или весь проект (но в настройках укажите `bot/` как root)

2. **Войдите на [render.com](https://render.com)** через GitHub

3. **Создайте Web Service:**
   - **New +** → **Web Service**
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий с ботом

4. **Настройки:**
   ```
   Name: betsbot-demo
   Environment: Node
   Region: Frankfurt (или ближайший)
   Branch: main
   Root Directory: bot  (если весь проект) или оставьте пустым (если только bot/)
   Build Command: npm install && npm run build
   Start Command: node dist/index.js
   ```

5. **Environment Variables:**
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   PORT=10000
   PUBLIC_WEBAPP_URL=https://ваш-домен.com/web  (если есть)
   PUBLIC_ADMIN_URL=https://ваш-домен.com/admin  (если есть)
   NODE_ENV=production
   ```

6. **Create Web Service** → ждите 2-5 минут

7. **Получите URL:** `https://betsbot-demo-xxxx.onrender.com`

8. **Проверьте:**
   - `https://betsbot-demo-xxxx.onrender.com/health` → должно вернуть `{"ok":true}`
   - `https://betsbot-demo-xxxx.onrender.com/api/w54/live` → должен вернуть матчи

---

### Вариант 2: Railway.app

**Плюсы:**
- ✅ $5 бесплатных кредитов в месяц (хватает для демо)
- ✅ Очень простой деплой
- ✅ Автоматический HTTPS

**Инструкция:**

1. Войдите на [railway.app](https://railway.app) через GitHub

2. **New Project** → **Deploy from GitHub repo**

3. Выберите репозиторий с `bot/`

4. Railway автоматически определит Node.js проект

5. **Добавьте переменные:**
   - `BOT_TOKEN`
   - `PORT` (Railway сам назначит, но можно указать)
   - `PUBLIC_WEBAPP_URL`
   - `PUBLIC_ADMIN_URL`

6. Готово! Railway даст вам URL типа `https://betsbot-production.up.railway.app`

---

### Вариант 3: Fly.io

**Плюсы:**
- ✅ Бесплатный тариф (3 shared-cpu-1x VMs)
- ✅ Глобальная сеть
- ✅ Не засыпает

**Инструкция:**

1. Установите Fly CLI:
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex
   ```

2. Войдите: `fly auth login`

3. В папке `bot/` создайте `fly.toml`:
   ```toml
   app = "betsbot-demo"
   primary_region = "fra"  # Frankfurt

   [build]

   [http_service]
     internal_port = 3000
     force_https = true
     auto_stop_machines = false
     auto_start_machines = true

   [[vm]]
     memory_mb = 512
   ```

4. Деплой:
   ```bash
   cd bot
   fly launch
   fly secrets set BOT_TOKEN=ваш_токен
   fly secrets set PUBLIC_WEBAPP_URL=https://ваш-домен.com/web
   fly secrets set PUBLIC_ADMIN_URL=https://ваш-домен.com/admin
   ```

---

### Вариант 4: Все на одном месте (Render + статический хостинг)

Если нужен и Frontend, и PHP API:

1. **Render** - для Node.js бота (бесплатно)
2. **Netlify/Vercel** - для Frontend (бесплатно)
3. **PlanetScale/Supabase** - для БД (бесплатно)

Или проще:
- **Render** - для бота
- **000webhost/InfinityFree** - для PHP + Frontend (бесплатно)

---

## 🔧 Настройка для демо

### 1. Обновите CORS в боте

В `bot/src/index.ts` уже есть:
```typescript
cors({
  origin: "*", // Разрешает все (для демо ОК)
  ...
})
```

Для продакшена лучше указать конкретные домены.

### 2. Настройте переменные окружения

На Render/Railway/Fly добавьте:
```
BOT_TOKEN=ваш_токен
PORT=10000  (или что назначит сервис)
PUBLIC_WEBAPP_URL=https://ваш-frontend.com/web  (если есть)
PUBLIC_ADMIN_URL=https://ваш-frontend.com/admin  (если есть)
```

### 3. Проверьте работу

После деплоя проверьте:
- ✅ `/health` - должен вернуть `{"ok":true}`
- ✅ `/api/w54/live` - должен вернуть матчи
- ✅ Бот в Telegram - отправьте `/start`

---

## 📱 Настройка Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather)

2. Если есть Frontend, настройте кнопки:
   ```
   /setmenubutton
   ```
   - `⚽ Ставки - https://ваш-frontend.com/web`
   - `🛠️ Админка - https://ваш-frontend.com/admin`

3. Если Frontend нет, бот все равно будет работать с командами

---

## ⚠️ Важно для демо

### Render Free Tier:

**Проблема:** Сервис "засыпает" после 15 минут неактивности

**Решения:**

1. **Использовать бесплатный мониторинг:**
   - [UptimeRobot](https://uptimerobot.com) - бесплатно, 50 мониторов
   - Настройте проверку каждые 5 минут: `https://ваш-сервис.onrender.com/health`

2. **Или использовать Railway/Fly.io** - они не засыпают

3. **Или Render Paid** - $7/месяц, не засыпает

### Puppeteer на бесплатном хостинге:

**Проблема:** Puppeteer требует много памяти

**Решения:**

1. **Render:** Обычно работает, но может быть медленно
2. **Railway:** Хватает на бесплатном тарифе
3. **Fly.io:** Нужно указать больше памяти в `fly.toml`

Если не работает, можно временно отключить Puppeteer и использовать обычный fetch (но может не парсить JS-контент).

---

## 🚀 Быстрый старт (Render)

1. **GitHub:**
   ```bash
   cd bot
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/betsbot.git
   git push -u origin main
   ```

2. **Render:**
   - New Web Service
   - Подключите репозиторий
   - Build: `npm install && npm run build`
   - Start: `node dist/index.js`
   - Добавьте переменные окружения
   - Deploy!

3. **Проверьте:**
   - Откройте URL от Render
   - Проверьте `/health`
   - Проверьте бота в Telegram

---

## 📊 Сравнение бесплатных вариантов

| Сервис | Бесплатно | Засыпает | Puppeteer | Простота |
|--------|-----------|----------|-----------|----------|
| **Render** | ✅ Да | ⚠️ Да (15 мин) | ✅ Работает | ⭐⭐⭐⭐⭐ |
| **Railway** | ✅ $5/мес | ❌ Нет | ✅ Работает | ⭐⭐⭐⭐⭐ |
| **Fly.io** | ✅ Да | ❌ Нет | ⚠️ Нужна настройка | ⭐⭐⭐ |
| **Heroku** | ❌ Нет | - | - | ⭐⭐⭐ |

**Рекомендация для демо:** **Render** - самый простой!

---

## 🎉 Готово!

После деплоя у вас будет:
- ✅ Telegram бот работает
- ✅ API для парсинга доступен
- ✅ Все на одном сервисе
- ✅ HTTPS из коробки
- ✅ Бесплатно!

**URL вашего API:** `https://ваш-сервис.onrender.com`
**Endpoints:**
- `/health` - проверка работы
- `/api/w54/live` - парсинг матчей
- `/api/w54/snapshot` - парсинг из файла
- `/api/w54/detail` - детали матча

---

## 🆘 Проблемы?

### Бот не запускается:
- Проверьте `BOT_TOKEN` в переменных окружения
- Проверьте логи в Render Dashboard

### API не отвечает:
- Проверьте, что сервис не "заснул" (сделайте запрос на `/health`)
- Проверьте логи на ошибки

### Puppeteer не работает:
- На Render обычно работает, но медленно
- Попробуйте Railway или Fly.io
- Или временно отключите Puppeteer в коде

---

## 💡 Следующие шаги

После успешного демо можно:
1. Добавить Frontend (Netlify/Vercel бесплатно)
2. Добавить PHP API (000webhost бесплатно)
3. Настроить мониторинг (UptimeRobot бесплатно)
4. Перейти на платный тариф для продакшена

# 🚀 Быстрый старт развертывания

## Краткая инструкция для Windows

### Шаг 1: Подготовка файлов

1. **Соберите Node.js бот:**
   ```cmd
   cd bot
   npm install
   npm run build
   ```

2. **Проверьте конфигурацию PHP:**
   - Откройте `api/config.php`
   - Обновите данные БД для вашего хостинга

### Шаг 2: Загрузка на веб-хостинг

**Что загружать:**
- Вся папка `api/` → `/api/` на хостинге
- Вся папка `web/` → `/web/` на хостинге  
- Вся папка `admin/` → `/admin/` на хостинге

**Как загружать:**
- Используйте FileZilla (FTP/SFTP)
- Или панель управления хостингом (файловый менеджер)

**Настройка БД:**
1. Откройте phpMyAdmin на хостинге
2. Создайте базу данных `betsbot_db`
3. Импортируйте `api/database.sql`
4. Импортируйте остальные SQL файлы из `api/`

### Шаг 3: Настройка VPS для Node.js бота

**Подключение:**
- Используйте PuTTY для SSH подключения к VPS

**Команды на VPS:**
```bash
# 1. Установка Node.js (если еще не установлен)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 2. Загрузка файлов бота
# Используйте FileZilla (SFTP) или SCP для загрузки папки bot/

# 3. Переход в папку бота
cd bot

# 4. Установка зависимостей
npm install --production

# 5. Сборка (если dist/ нет)
npm run build

# 6. Создание .env файла
nano .env
# Скопируйте содержимое из bot/.env.production.example
# Заполните реальные значения

# 7. Установка PM2
npm install -g pm2

# 8. Запуск бота
pm2 start dist/index.js --name betsbot
pm2 save
pm2 startup
```

### Шаг 4: Настройка Nginx (если нужен прокси)

Если хотите, чтобы Node.js API был доступен через ваш домен:

1. Скопируйте `nginx-example.conf` в `/etc/nginx/sites-available/betsbot`
2. Отредактируйте под ваш домен
3. Создайте симлинк: `sudo ln -s /etc/nginx/sites-available/betsbot /etc/nginx/sites-enabled/`
4. Проверьте конфигурацию: `sudo nginx -t`
5. Перезагрузите: `sudo systemctl reload nginx`

### Шаг 5: Настройка SSL (обязательно!)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx

# Получение сертификата
sudo certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

### Шаг 6: Настройка Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots` → выберите вашего бота
3. Выберите "Bot Settings" → "Menu Button"
4. Добавьте:
   - "⚽ Ставки" → `https://ваш-домен.com/web`
   - "🛠️ Админка" → `https://ваш-домен.com/admin`

### Шаг 7: Проверка

1. ✅ Откройте `https://ваш-домен.com/web` - должен загрузиться интерфейс
2. ✅ Откройте `https://ваш-домен.com/api/matches.php` - должен вернуться JSON
3. ✅ Откройте бота в Telegram → `/start` - должны появиться кнопки
4. ✅ Проверьте логи бота: `pm2 logs betsbot` на VPS

---

## ⚠️ Важные моменты

- **HTTPS обязателен** для Telegram WebApp
- **Бот должен работать 24/7** - используйте PM2
- **Не загружайте .env файлы** в публичные места
- **Минимум 2GB RAM** на VPS для Puppeteer

---

## 🆘 Проблемы?

Смотрите подробное руководство в `DEPLOYMENT.md`

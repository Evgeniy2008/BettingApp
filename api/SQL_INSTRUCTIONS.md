# 📋 Инструкция по использованию SQL файлов

## 🎯 Какой файл использовать?

### `create_all_tables.sql` - Полный файл (рекомендуется)
**Используйте для:**
- Создания новой базы данных с нуля
- Первой установки системы
- Полной переустановки

**Что делает:**
- Создает базу данных `betsbot_db`
- Создает все таблицы
- Добавляет начальные данные (пароль админа: `admin123`)

**Как использовать:**
1. Откройте phpMyAdmin или MySQL клиент
2. Выполните весь файл целиком
3. Готово!

---

### `create_all_tables_no_db.sql` - Без создания БД
**Используйте для:**
- Когда база данных уже создана
- Когда нужно только создать таблицы
- Для обновления структуры (если таблицы не существуют)

**Как использовать:**
1. Убедитесь, что база данных создана
2. Выберите базу: `USE betsbot_db;`
3. Выполните файл

---

## 📊 Создаваемые таблицы

### 1. `users` - Пользователи
- Основная информация о пользователях
- Баланс, кредитный лимит, долг
- Telegram ID и username
- Session token для авторизации

### 2. `deposits` - Заявки на пополнение
- Заявки пользователей на пополнение баланса
- Статусы: pending, approved, rejected
- Поддержка разных валют (USDT по умолчанию)

### 3. `withdrawals` - Заявки на вывод
- Заявки пользователей на вывод средств
- Статусы: pending, approved, rejected
- Поддержка разных валют

### 4. `bets` - Ставки
- Все ставки пользователей
- Поддержка одиночных и экспресс-ставок
- Статусы: pending, active, won, lost, cancelled, refunded
- Поле `fixture_id` для связи с API-Sports
- Поле `bet_details` (JSON) для экспресс-ставок

### 5. `admin_settings` - Настройки админки
- Пароль администратора
- Адреса кошельков для пополнения
- Другие настройки системы

### 6. `admin_sessions` - Сессии администраторов
- Токены сессий для админ-панели
- Автоматическое истечение сессий

### 7. `credit_requests` - Запросы на кредит
- Запросы пользователей на увеличение кредитного лимита
- Статусы: pending, approved, rejected

---

## 🔧 Использование через phpMyAdmin

### Вариант 1: Импорт файла
1. Войдите в phpMyAdmin
2. Выберите вашу базу данных (или создайте новую)
3. Перейдите на вкладку **"Импорт"**
4. Нажмите **"Выберите файл"** и выберите `create_all_tables.sql`
5. Нажмите **"Вперед"**

### Вариант 2: SQL запрос
1. Войдите в phpMyAdmin
2. Выберите вашу базу данных
3. Перейдите на вкладку **"SQL"**
4. Скопируйте содержимое файла `create_all_tables.sql`
5. Вставьте в поле и нажмите **"Вперед"**

---

## 🔧 Использование через командную строку

```bash
# Вариант 1: Создание новой БД
mysql -u root -p < api/create_all_tables.sql

# Вариант 2: Только таблицы (БД уже существует)
mysql -u root -p betsbot_db < api/create_all_tables_no_db.sql

# Вариант 3: С указанием пользователя
mysql -u betsbot_user -p betsbot_db < api/create_all_tables.sql
```

---

## ✅ Проверка после создания

### Проверка таблиц:
```sql
USE betsbot_db;
SHOW TABLES;
```

Должно показать 7 таблиц:
- users
- deposits
- withdrawals
- bets
- admin_settings
- admin_sessions
- credit_requests

### Проверка структуры:
```sql
DESCRIBE users;
DESCRIBE bets;
DESCRIBE deposits;
```

### Проверка начальных данных:
```sql
SELECT * FROM admin_settings;
```

Должен быть пароль админа: `admin123`

---

## 🔒 Безопасность

### После создания БД:

1. **Измените пароль админа:**
   ```sql
   UPDATE admin_settings 
   SET setting_value = 'ваш_новый_пароль' 
   WHERE setting_key = 'admin_password';
   ```

2. **Настройте адрес кошелька:**
   ```sql
   UPDATE admin_settings 
   SET setting_value = 'ваш_USDT_адрес' 
   WHERE setting_key = 'deposit_wallet_usdt';
   ```

3. **Создайте отдельного пользователя БД** (не root):
   ```sql
   CREATE USER 'betsbot_user'@'localhost' IDENTIFIED BY 'сильный_пароль';
   GRANT ALL PRIVILEGES ON betsbot_db.* TO 'betsbot_user'@'localhost';
   FLUSH PRIVILEGES;
   ```

---

## 🆘 Решение проблем

### Ошибка: "Table already exists"
- Это нормально, если таблицы уже существуют
- Файл использует `CREATE TABLE IF NOT EXISTS`, поэтому безопасен
- Или используйте `DROP TABLE IF EXISTS` перед созданием (осторожно - удалит данные!)

### Ошибка: "Access denied"
- Проверьте права пользователя БД
- Убедитесь, что пользователь имеет права CREATE, ALTER, INSERT

### Ошибка: "Unknown database"
- Сначала создайте базу данных вручную
- Или используйте `create_all_tables.sql` (он создает БД автоматически)

### Ошибка с FOREIGN KEY
- Убедитесь, что таблица `users` создана первой
- Проверьте, что используется InnoDB (не MyISAM)

---

## 📝 Дополнительные миграции

Если нужно добавить поля к существующим таблицам, используйте отдельные миграции:

- `add_fixture_id_to_bets.sql` - добавляет поле fixture_id
- `add_credit_system.sql` - добавляет поле current_debt
- `add_currency_to_deposits.sql` - добавляет поле currency
- `add_bet_details_field.sql` - добавляет поле bet_details

**Но в `create_all_tables.sql` все эти поля уже включены!**

---

## 🎉 Готово!

После выполнения SQL файла:
1. ✅ База данных создана
2. ✅ Все таблицы созданы
3. ✅ Начальные данные добавлены
4. ✅ Можно настраивать `api/config.php` и запускать приложение

---

**Вопросы?** Проверьте логи ошибок в phpMyAdmin или MySQL логах.

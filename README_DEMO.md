# 🎯 Как это работает

## Структура проекта

```
betsBot/
├── bot/                    ← ВСЕ ЗДЕСЬ!
│   ├── src/
│   │   ├── index.ts       ← Telegram бот + Express API + Парсинг
│   │   └── parsers/       ← Парсеры матчей
│   ├── package.json
│   └── ...
├── api/                    ← PHP API (для ставок, кошелька)
├── web/                    ← Frontend (если нужен)
└── admin/                  ← Админка (если нужна)
```

## 🔑 Главное понимание

**Бот и парсинг УЖЕ вместе!**

В файле `bot/src/index.ts`:
- ✅ Express сервер (API)
- ✅ Telegram бот (Telegraf)
- ✅ Endpoints для парсинга (`/api/w54/live`, `/api/w54/snapshot`)
- ✅ Все работает на одном порту

**Вам НЕ нужно ничего связывать** - все уже связано!

---

## 🚀 Для демо нужно:

### Минимум (только бот + парсинг):
1. Загрузить `bot/` на Render/Railway/Fly.io
2. Указать `BOT_TOKEN`
3. Готово!

### Полный вариант (с Frontend):
1. `bot/` → Render (Node.js)
2. `api/` + `web/` + `admin/` → Hostinger/000webhost (PHP + статика)
3. Связать через URL в `web/app.js`

---

## 📖 Инструкции

- **QUICK_DEMO.md** - быстрый старт (5 минут)
- **DEPLOY_FREE_DEMO.md** - подробная инструкция
- **DEPLOY_HOSTINGER_RENDER.md** - если нужен PHP + Frontend

---

## 💡 Рекомендация для демо

**Самый простой вариант:**
1. Загрузите `bot/` на **Render** (бесплатно)
2. Настройте UptimeRobot для пробуждения (бесплатно)
3. Готово! Бот работает, API работает, парсинг работает

**Все в одном месте, ничего связывать не нужно!**

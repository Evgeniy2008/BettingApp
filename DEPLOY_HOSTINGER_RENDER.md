# Развертывание: Hostinger (PHP) + Render (Node.js)

Этот вариант идеально подходит, если у вас:
- **Hostinger** - для PHP сайта и Frontend
- **Render** - для Node.js бота (бесплатный тариф доступен!)

---

## 📦 Часть 1: Развертывание на Hostinger

### Шаг 1: Подготовка файлов

1. **Соберите файлы для загрузки:**
   - Папка `api/` - все PHP файлы
   - Папка `web/` - Frontend приложение
   - Папка `admin/` - Админ панель

2. **Обновите `api/config.php`:**
   ```php
   define('DB_HOST', 'localhost'); // Обычно localhost на Hostinger
   define('DB_NAME', 'u123456789_betsbot'); // Ваше имя БД из панели
   define('DB_USER', 'u123456789_user'); // Ваш пользователь БД
   define('DB_PASS', 'ваш_пароль'); // Ваш пароль БД
   ```

### Шаг 2: Загрузка на Hostinger

1. **Войдите в панель Hostinger** (hPanel)

2. **Откройте File Manager** или используйте FTP (FileZilla)

3. **Загрузите файлы в `public_html/`:**
   ```
   public_html/
   ├── api/
   │   ├── config.php (обновленный!)
   │   ├── matches.php
   │   ├── bets.php
   │   └── ... (все PHP файлы)
   ├── web/
   │   ├── index.html
   │   ├── app.js
   │   ├── wallet.js
   │   └── styles.css
   └── admin/
       ├── index.html
       ├── app.js
       └── styles.css
   ```

4. **Создайте базу данных:**
   - В hPanel откройте **MySQL Databases**
   - Создайте новую базу данных (например, `betsbot_db`)
   - Создайте пользователя и назначьте права
   - Запомните данные: имя БД, пользователь, пароль

5. **Импортируйте SQL:**
   - Откройте **phpMyAdmin** в hPanel
   - Выберите вашу базу данных
   - Импортируйте файлы по порядку:
     1. `api/database.sql` (основная структура)
     2. `api/create_bets_table.sql`
     3. `api/add_credit_system.sql`
     4. `api/add_currency_to_deposits.sql`
     5. Остальные миграции при необходимости

6. **Проверьте работу PHP API:**
   - Откройте: `https://ваш-домен.com/api/matches.php`
   - Должен вернуться JSON (может быть пустым, но без ошибок)

---

## 🚀 Часть 2: Развертывание на Render

### Шаг 1: Подготовка репозитория

**Вариант A: Через Git (рекомендуется)**

1. Создайте репозиторий на GitHub/GitLab
2. Загрузите только папку `bot/` в репозиторий
3. Или создайте отдельный репозиторий только для бота

**Вариант B: Прямая загрузка**

Можно загрузить файлы напрямую, но через Git удобнее для обновлений.

### Шаг 2: Создание сервиса на Render

1. **Войдите на [render.com](https://render.com)** (можно через GitHub)

2. **Создайте новый Web Service:**
   - Нажмите **"New +"** → **"Web Service"**
   - Подключите ваш репозиторий (или загрузите файлы)

3. **Настройки сервиса:**
   ```
   Name: betsbot
   Environment: Node
   Region: Frankfurt (или ближайший к вам)
   Branch: main (или master)
   Root Directory: bot (если репозиторий корневой, или оставьте пустым если только bot/)
   Build Command: cd bot && npm install && npm run build
   Start Command: cd bot && node dist/index.js
   ```

4. **Переменные окружения (Environment Variables):**
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   PORT=10000
   PUBLIC_WEBAPP_URL=https://ваш-домен.com/web
   PUBLIC_ADMIN_URL=https://ваш-домен.com/admin
   NODE_ENV=production
   ```

   ⚠️ **Важно:** Render автоматически назначает порт через переменную `PORT`, но в коде может быть другое значение. Проверьте `bot/src/index.ts`.

5. **Нажмите "Create Web Service"**

6. **Дождитесь деплоя** (обычно 2-5 минут)

7. **Получите URL сервиса:**
   - После деплоя Render даст URL типа: `https://betsbot-xxxx.onrender.com`
   - Этот URL будет вашим Node.js API

### Шаг 3: Обновление CORS в боте

В `bot/src/index.ts` убедитесь, что CORS разрешает ваш домен Hostinger:

```typescript
app.use(
  cors({
    origin: [
      "https://ваш-домен.com",
      "https://www.ваш-домен.com",
      "http://localhost:5173", // для локальной разработки
    ],
    methods: ["GET", "POST", "OPTIONS"],
    allowedHeaders: ["Content-Type"]
  })
);
```

Если нужно разрешить все домены (для разработки):
```typescript
origin: "*" // Только для тестирования! В продакшене лучше указать конкретные домены
```

---

## 🔗 Часть 3: Связывание компонентов

### Обновление Frontend (web/app.js)

В файле `web/app.js` нужно указать URL вашего Render сервиса:

```javascript
// API endpoint - Render сервис
const isProduction = window.location.protocol === 'https:' || 
                     !window.location.hostname.includes('localhost');
const API_BASE = isProduction 
  ? "https://betsbot-xxxx.onrender.com"  // URL вашего Render сервиса!
  : "http://localhost:3000"; // Development
```

**Или лучше:** Используйте переменную окружения или конфигурационный файл.

### Альтернатива: Прокси через Hostinger

Если хотите, чтобы API был на том же домене (удобнее для CORS):

1. **Создайте файл `api/proxy.php` на Hostinger:**
   ```php
   <?php
   header('Content-Type: application/json');
   header('Access-Control-Allow-Origin: *');
   
   $renderUrl = 'https://betsbot-xxxx.onrender.com';
   $path = $_GET['path'] ?? '';
   $query = $_SERVER['QUERY_STRING'] ?? '';
   
   $url = $renderUrl . '/' . $path;
   if ($query && strpos($path, '?') === false) {
       $url .= '?' . $query;
   }
   
   $ch = curl_init();
   curl_setopt($ch, CURLOPT_URL, $url);
   curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
   curl_setopt($ch, CURLOPT_TIMEOUT, 60);
   curl_setopt($ch, CURLOPT_FOLLOWLOCATION, true);
   
   $response = curl_exec($ch);
   $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
   curl_close($ch);
   
   http_response_code($httpCode);
   echo $response;
   ```

2. **Тогда в `web/app.js`:**
   ```javascript
   const API_BASE = isProduction 
     ? window.location.origin + "/api/proxy.php?path=api"  // Через прокси
     : "http://localhost:3000";
   ```

---

## ⚙️ Часть 4: Настройка Telegram бота

1. **Откройте [@BotFather](https://t.me/BotFather)**

2. **Настройте кнопки меню:**
   ```
   /setmenubutton
   ```
   Выберите вашего бота, затем:
   ```
   ⚽ Ставки - https://ваш-домен.com/web
   🛠️ Админка - https://ваш-домен.com/admin
   ```

3. **Проверьте работу:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Должны появиться кнопки WebApp

---

## 🔧 Часть 5: Настройка Render для автодеплоя

Render автоматически деплоит при пуше в Git, но можно настроить:

1. **В настройках сервиса Render:**
   - **Auto-Deploy:** Yes (автоматический деплой при push)
   - **Pull Request Previews:** Yes (опционально, для тестирования)

2. **Для обновления бота:**
   - Сделайте изменения в коде
   - Закоммитьте и запушьте в Git
   - Render автоматически пересоберет и перезапустит сервис

---

## ⚠️ Важные моменты

### Render Free Tier ограничения:

- **Сервис "засыпает" после 15 минут неактивности**
- Первый запрос после простоя может занять 30-60 секунд (холодный старт)
- Для продакшена лучше использовать **Paid Plan** ($7/месяц) - без простоев

### Решения проблемы "засыпания":

1. **Использовать бесплатный мониторинг:**
   - Настройте cron job на Hostinger, который будет "будить" Render каждые 10 минут
   - Создайте файл `api/ping-render.php`:
   ```php
   <?php
   $renderUrl = 'https://betsbot-xxxx.onrender.com/health';
   file_get_contents($renderUrl);
   echo "Pinged Render";
   ```
   - Настройте Cron Job в hPanel на выполнение каждые 10 минут

2. **Или использовать Render Paid Plan** - сервис не будет засыпать

### Безопасность:

- Не коммитьте `.env` файлы в Git
- Используйте Environment Variables в Render
- Используйте HTTPS везде (Hostinger обычно предоставляет бесплатный SSL)

---

## 📋 Чек-лист развертывания

### Hostinger:
- [ ] Загружены папки `api/`, `web/`, `admin/`
- [ ] Обновлен `api/config.php` с данными БД
- [ ] Создана MySQL база данных
- [ ] Импортированы SQL файлы
- [ ] Проверен доступ к `https://ваш-домен.com/api/matches.php`
- [ ] Настроен SSL (обычно автоматически на Hostinger)

### Render:
- [ ] Создан Web Service
- [ ] Настроены переменные окружения
- [ ] Указан правильный Build Command
- [ ] Указан правильный Start Command
- [ ] Сервис успешно задеплоен
- [ ] Проверен доступ к `https://betsbot-xxxx.onrender.com/health`

### Связывание:
- [ ] Обновлен `web/app.js` с URL Render сервиса
- [ ] Настроен CORS в `bot/src/index.ts`
- [ ] Протестирован доступ Frontend к Node.js API

### Telegram:
- [ ] Настроены кнопки в BotFather
- [ ] Протестирован бот в Telegram
- [ ] WebApp кнопки открываются корректно

---

## 🆘 Решение проблем

### Render сервис не запускается:
- Проверьте логи в Render Dashboard
- Убедитесь, что `Start Command` правильный
- Проверьте, что `dist/index.js` существует после сборки

### CORS ошибки:
- Проверьте настройки CORS в `bot/src/index.ts`
- Убедитесь, что домен Hostinger добавлен в `origin`

### Бот не отвечает:
- Проверьте переменную `BOT_TOKEN` в Render
- Проверьте логи: `pm2 logs` (если используете) или логи в Render Dashboard

### API не доступен:
- Проверьте, что сервис на Render запущен (не "заснул")
- Проверьте URL в `web/app.js`
- Проверьте файрвол (Render обычно не требует настройки)

---

## 💰 Стоимость

- **Hostinger:** от ~$2-5/месяц (зависит от тарифа)
- **Render Free:** бесплатно (но с ограничениями)
- **Render Paid:** $7/месяц (без ограничений)

**Итого:** от $2/месяц (если использовать Render Free) до $12/месяц (Hostinger + Render Paid)

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет работать:
- PHP API на Hostinger
- Frontend на Hostinger
- Node.js бот на Render
- Все связано и работает!

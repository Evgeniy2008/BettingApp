# Руководство по развертыванию BetsBot на хостинге

## 📋 Структура проекта

Ваше приложение состоит из трех основных компонентов:

1. **PHP Backend** (`api/`) - работает с MySQL базой данных
2. **Node.js Bot** (`bot/`) - Telegram бот + API для парсинга матчей
3. **Frontend** (`web/`, `admin/`) - веб-интерфейсы

---

## 🎯 План развертывания

### Вариант 1: Обычный веб-хостинг (рекомендуется для начала)

**Что нужно:**
- Веб-хостинг с поддержкой PHP и MySQL (например, Timeweb, Beget, REG.RU)
- Отдельный VPS/VDS сервер для Node.js бота (например, Timeweb VPS, Selectel, DigitalOcean)

**Что куда загружать:**

#### На веб-хостинг (PHP + Frontend):

1. **Загрузите PHP файлы:**
   - Вся папка `api/` → загрузить в корень сайта или в папку `/api/`
   - Файлы: `matches.php`, `bets.php`, `auth.php`, `admin.php`, `wallet.php`, `config.php` и т.д.

2. **Загрузите Frontend:**
   - Папка `web/` → загрузить в корень сайта или в папку `/web/`
   - Папка `admin/` → загрузить в папку `/admin/`

3. **Настройте базу данных:**
   - В phpMyAdmin создайте базу данных `betsbot_db`
   - Импортируйте SQL файлы из папки `api/`:
     - `database.sql` (основная структура)
     - `create_bets_table.sql`
     - `add_credit_system.sql`
     - и другие миграции

4. **Настройте `api/config.php`:**
   ```php
   define('DB_HOST', 'localhost'); // или IP MySQL сервера
   define('DB_NAME', 'betsbot_db');
   define('DB_USER', 'ваш_пользователь_бд');
   define('DB_PASS', 'ваш_пароль_бд');
   ```

#### На VPS сервер (Node.js бот):

1. **Подключитесь к серверу по SSH** (используйте PuTTY на Windows)

2. **Установите Node.js:**
   ```bash
   # На Ubuntu/Debian
   curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
   sudo apt-get install -y nodejs
   
   # Проверьте установку
   node --version
   npm --version
   ```

3. **Загрузите папку `bot/` на сервер:**
   - Используйте FileZilla (SFTP) или SCP
   - Или клонируйте через Git, если есть репозиторий

4. **Установите зависимости:**
   ```bash
   cd bot
   npm install
   ```

5. **Соберите TypeScript:**
   ```bash
   npm run build
   ```

6. **Создайте файл `.env` в папке `bot/`:**
   ```env
   BOT_TOKEN=ваш_токен_от_BotFather
   PORT=3000
   
   # URL вашего веб-приложения (HTTPS обязательно!)
   PUBLIC_WEBAPP_URL=https://ваш-домен.com/web
   PUBLIC_ADMIN_URL=https://ваш-домен.com/admin
   ```

7. **Установите PM2 для автозапуска:**
   ```bash
   npm install -g pm2
   
   # Запустите бота
   pm2 start dist/index.js --name betsbot
   
   # Сохраните конфигурацию для автозапуска
   pm2 save
   pm2 startup
   ```

8. **Настройте файрвол (если нужно):**
   ```bash
   # Откройте порт 3000 для API (если нужен доступ извне)
   sudo ufw allow 3000/tcp
   ```

---

### Вариант 2: Все на одном VPS (если хостинг поддерживает Node.js)

Если ваш хостинг поддерживает Node.js (например, некоторые VPS с панелью управления):

1. **Загрузите все файлы на сервер:**
   - `api/` → `/var/www/html/api/` или `/home/user/public_html/api/`
   - `web/` → `/var/www/html/web/`
   - `admin/` → `/var/www/html/admin/`
   - `bot/` → `/home/user/betsbot/bot/`

2. **Настройте веб-сервер (Nginx):**
   ```nginx
   server {
       listen 80;
       server_name ваш-домен.com;
       
       # PHP API
       location /api {
           root /var/www/html;
           try_files $uri $uri/ /api/index.php?$query_string;
       }
       
       # Frontend
       location / {
           root /var/www/html/web;
           try_files $uri $uri/ /index.html;
       }
       
       location /admin {
           root /var/www/html;
           try_files $uri $uri/ /admin/index.html;
       }
       
       # Node.js API (проксирование)
       location /api/w54 {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

3. **Настройте SSL (обязательно для Telegram WebApp):**
   - Используйте Let's Encrypt (бесплатный SSL)
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d ваш-домен.com
   ```

---

## 🔧 Настройка после развертывания

### 1. Обновите URL в Frontend

В файле `web/app.js` измените:
```javascript
// Было:
const API_BASE = "http://localhost:3000";

// Должно быть (если Node.js API на том же домене через прокси):
const API_BASE = window.location.origin;

// Или если Node.js на отдельном сервере:
const API_BASE = "https://ваш-nodejs-сервер.com:3000";
```

### 2. Настройте CORS в Node.js боте

В файле `bot/src/index.ts` уже есть настройки CORS, но убедитесь, что они разрешают ваш домен:
```typescript
cors({
  origin: ["https://ваш-домен.com", "https://www.ваш-домен.com"],
  methods: ["GET", "POST", "OPTIONS"],
  allowedHeaders: ["Content-Type"]
})
```

### 3. Настройте Telegram Bot

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots` → выберите вашего бота
3. Выберите "Bot Settings" → "Menu Button"
4. Добавьте кнопки:
   - "⚽ Ставки" → `https://ваш-домен.com/web`
   - "🛠️ Админка" → `https://ваш-домен.com/admin`

Или используйте команду:
```
/setmenubutton
```
Выберите бота, затем:
```
⚽ Ставки - https://ваш-домен.com/web
🛠️ Админка - https://ваш-домен.com/admin
```

---

## 📦 Что загружать на хостинг

### Минимальный набор файлов:

**Для веб-хостинга:**
```
api/
  ├── *.php (все PHP файлы)
  ├── config.php (настроить!)
web/
  ├── index.html
  ├── app.js
  ├── wallet.js
  ├── styles.css
admin/
  ├── index.html
  ├── app.js
  ├── styles.css
  └── styles_enhancements.css
```

**Для VPS (Node.js бот):**
```
bot/
  ├── dist/ (после сборки)
  ├── package.json
  ├── .env (создать на сервере!)
  └── node_modules/ (установить через npm install)
```

**НЕ загружайте:**
- `node_modules/` (установите на сервере через `npm install`)
- `.env` файлы с реальными токенами (создайте на сервере)
- `bot/data/betsbot.db` (если используется SQLite, но у вас MySQL)
- Локальные файлы разработки (`testing.html`, `Parseinfo.html` и т.д.)

---

## 🚀 Проверка работы

1. **Проверьте PHP API:**
   - Откройте: `https://ваш-домен.com/api/matches.php`
   - Должен вернуться JSON

2. **Проверьте Node.js API:**
   - Откройте: `https://ваш-домен.com/api/w54/live` (если через прокси)
   - Или: `http://ваш-vps-ip:3000/api/w54/live`
   - Должен вернуться JSON с матчами

3. **Проверьте Frontend:**
   - Откройте: `https://ваш-домен.com/web`
   - Должен загрузиться интерфейс

4. **Проверьте бота:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Должны появиться кнопки WebApp

---

## ⚠️ Важные моменты

1. **HTTPS обязателен** для Telegram WebApp! Без HTTPS кнопки не будут работать.

2. **Бот должен работать 24/7** - используйте PM2 или systemd для автозапуска.

3. **Безопасность:**
   - Не загружайте `.env` файлы в Git
   - Используйте сильные пароли для БД
   - Настройте файрвол на VPS

4. **Производительность:**
   - Node.js бот использует Puppeteer (Chrome), что требует ресурсов
   - Рекомендуется минимум 2GB RAM на VPS для бота

---

## 🆘 Решение проблем

### Бот не запускается:
```bash
# Проверьте логи
pm2 logs betsbot

# Проверьте, что порт свободен
netstat -tulpn | grep 3000
```

### API не отвечает:
- Проверьте, что Node.js процесс запущен: `pm2 list`
- Проверьте файрвол: `sudo ufw status`
- Проверьте логи: `pm2 logs betsbot`

### Telegram WebApp не открывается:
- Убедитесь, что используется HTTPS
- Проверьте настройки в BotFather
- Проверьте URL в `.env` файле бота

### Ошибки подключения к БД:
- Проверьте настройки в `api/config.php`
- Убедитесь, что MySQL сервер доступен
- Проверьте права пользователя БД

---

## 📝 Чек-лист развертывания

- [ ] Загружены PHP файлы на веб-хостинг
- [ ] Загружены Frontend файлы (web/, admin/)
- [ ] Создана и настроена MySQL база данных
- [ ] Обновлен `api/config.php` с данными БД
- [ ] Загружен Node.js бот на VPS
- [ ] Установлены зависимости (`npm install`)
- [ ] Собран TypeScript (`npm run build`)
- [ ] Создан `.env` файл с токенами
- [ ] Запущен бот через PM2
- [ ] Настроен SSL (HTTPS)
- [ ] Обновлены URL в `web/app.js`
- [ ] Настроены кнопки в BotFather
- [ ] Протестированы все компоненты

---

## 💡 Альтернативные варианты

Если не хотите настраивать VPS вручную, можно использовать:

1. **Heroku** (бесплатный тариф отменен, но есть платные)
2. **Railway** (удобный деплой Node.js)
3. **Render** (бесплатный тариф для Node.js)
4. **DigitalOcean App Platform** (простой деплой)

Эти сервисы автоматически развернут Node.js приложение, но потребуется настроить переменные окружения.
